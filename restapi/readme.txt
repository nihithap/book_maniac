1)springcoresample: 
beans.xml in resouces
in app. =classpathxmlapplicationcontex,application context

dependency injection: property,constructor,setter
autowire:by type, byname,constructor

2) springcorejavaconf:
without using bean.xml file we are using javafile
in java file we are writting (annotateconfigapplicationcontext) instead of classpathxml


3)simpleSpringmvc:
in src-controller
in webapp-webinf-create folder views->move index.jsp to views folder
web-inf->dispatcher-servlet.xml ->spring-context.fsd
                                ->locate controller
                                ->viewresolver
       ->web.xml: create servlet name and servlet mapping



4)samplebookmv:
to see the output we need touse tomcat server:
in servers,tomact server before starting tomcat we add the project we want to run and start the server

views folder ->we use index.jsp,welcome.jsp basically we are using jsp files as client side code (i.e like html)
webinf-displacter servlet,web.xml

in src-controller---> for all post,get,put,delete mapping
src-repo-> define all crud
src-model

5)samplemvcjava:
6)samplejdbctemp:
7)customer orm sample:

8)reatsamples:
9)springboot:
